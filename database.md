-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.colaboradores (
  cpf character varying NOT NULL,
  ind text,
  status text,
  enviado_rh text,
  pessoa text,
  req text,
  vinculado text,
  carta_oferta text,
  colab_pend text,
  exame text,
  clinica text,
  docs text,
  aso text,
  rpv text,
  pre_admissao text,
  mob text,
  op text,
  data_admissao date,
  contrato text,
  portal text,
  cracha text,
  ponto text,
  treinamento text,
  realizar_treinamento text,
  local_treinamento text,
  re text,
  nome text NOT NULL,
  funcao_clt text,
  histograma text,
  idade integer,
  dt_nascimento date,
  vr text,
  termino date,
  prorrogacao date,
  demissao date,
  municipio text,
  uf text,
  telefone text,
  turno_trabalho text,
  created_at timestamp with time zone DEFAULT timezone('utc'::text, now()),
  check_in date,
  hotel text,
  data_viagem date,
  CONSTRAINT colaboradores_pkey PRIMARY KEY (cpf)
);
CREATE TABLE public.configuracoes (
  id integer NOT NULL DEFAULT 1,
  dias_totais_projeto integer,
  data_inicio_projeto date,
  data_fim_projeto date,
  meta_admissoes integer,
  gerente_operacoes text,
  gerente_contrato text,
  nome_cliente text,
  centro_custo text,
  colaboradores_previstos integer,
  orcado_suprimentos numeric,
  feriados_projeto ARRAY,
  updated_at timestamp with time zone DEFAULT timezone('utc'::text, now()),
  CONSTRAINT configuracoes_pkey PRIMARY KEY (id)
);
CREATE TABLE public.configuracoes_clinicas (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  nome text NOT NULL UNIQUE,
  endereco text,
  cidade text,
  ativo boolean DEFAULT true,
  CONSTRAINT configuracoes_clinicas_pkey PRIMARY KEY (id)
);
CREATE TABLE public.configuracoes_hoteis (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  nome text NOT NULL UNIQUE,
  qt_vagas integer NOT NULL DEFAULT 0,
  vagas_ocupadas integer NOT NULL DEFAULT 0,
  projeto_id uuid DEFAULT '00000000-0000-0000-0000-000000000000'::uuid,
  CONSTRAINT configuracoes_hoteis_pkey PRIMARY KEY (id)
);
CREATE TABLE public.etapas (
  id integer NOT NULL DEFAULT nextval('etapas_id_seq'::regclass),
  config_id integer DEFAULT 1,
  nome text NOT NULL,
  dias integer NOT NULL,
  concluida boolean DEFAULT false,
  ordem integer,
  percentual_concluido numeric,
  CONSTRAINT etapas_pkey PRIMARY KEY (id),
  CONSTRAINT etapas_config_id_fkey FOREIGN KEY (config_id) REFERENCES public.configuracoes(id)
);
CREATE TABLE public.logistica_controle (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  cpf character varying UNIQUE,
  re character varying,
  nome character varying,
  turno_semana character varying,
  turno_sabado character varying,
  turno_domingo character varying,
  hotel character varying,
  quarto character varying,
  data_checkin date,
  tipo_transporte character varying,
  rota_transporte character varying,
  created_at timestamp with time zone DEFAULT timezone('utc'::text, now()),
  cracha character varying,
  funcao_clt character varying,
  mob character varying,
  portal character varying,
  ponto character varying,
  turno_trabalho character varying,
  coordenador text,
  supervisor text,
  encarregado text,
  data_admissao date,
  sexo text,
  tipo_apto text,
  c_custos_hospedagem text,
  data_nasc date,
  telefone text,
  municipio text,
  uf text,
  data_nascimento date,
  demissao date,
  fase character varying,
  local_trabalho character varying,
  status text,
  setor_trabalho text,
  endereco text,
  bairro text,
  vr text,
  check_in date,
  check_out date,
  idade integer,
  lanche text,
  num_apto text,
  situacao text,
  CONSTRAINT logistica_controle_pkey PRIMARY KEY (id)
);
CREATE TABLE public.logs_auditoria (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  created_at timestamp with time zone DEFAULT now(),
  usuario text NOT NULL,
  acao text NOT NULL,
  detalhes text NOT NULL,
  cpf_colaborador text,
  CONSTRAINT logs_auditoria_pkey PRIMARY KEY (id)
);
CREATE TABLE public.rh_colaboradores (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  cpf character varying NOT NULL UNIQUE,
  re character varying,
  nome character varying NOT NULL,
  funcao_clt character varying,
  status character varying,
  aso character varying,
  mob character varying,
  data_admissao date,
  termino date,
  demissao date,
  municipio character varying,
  uf character varying,
  created_at timestamp with time zone DEFAULT timezone('utc'::text, now()),
  CONSTRAINT rh_colaboradores_pkey PRIMARY KEY (id)
);
CREATE TABLE public.seguranca_fits (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  re character varying UNIQUE,
  nome character varying,
  funcao_clt character varying,
  n_fit character varying,
  status_portal character varying,
  data_cracha_retirado date,
  created_at timestamp with time zone DEFAULT timezone('utc'::text, now()),
  aso character varying,
  treinamento character varying,
  cpf character varying,
  clinica character varying,
  exame character varying,
  docs character varying,
  realizar_treinamento character varying,
  local_treinamento character varying,
  data_admissao date,
  status character varying,
  mob text,
  rpv text,
  municipio text,
  uf text,
  num_fit text,
  CONSTRAINT seguranca_fits_pkey PRIMARY KEY (id)
);
CREATE TABLE public.suprimentos_ordens (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  item character varying,
  prioridade character varying,
  descricao text,
  fornecedor character varying,
  requisitante character varying,
  status character varying,
  ordem_compra character varying,
  valor_oc numeric,
  status_ordem character varying,
  entregue_obra boolean DEFAULT false,
  created_at timestamp with time zone DEFAULT timezone('utc'::text, now()),
  total_req_previstas numeric DEFAULT 0,
  valores numeric DEFAULT 0,
  requisicao character varying,
  data_criacao date,
  cotacoes text,
  informado_por text,
  fornecedores text,
  CONSTRAINT suprimentos_ordens_pkey PRIMARY KEY (id)
);
CREATE TABLE public.usuarios_permitidos (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  re text NOT NULL UNIQUE,
  nome text NOT NULL,
  perfil text NOT NULL DEFAULT 'user'::text CHECK (perfil = ANY (ARRAY['admin'::text, 'user'::text, 'guest'::text])),
  autorizado_em timestamp with time zone DEFAULT now(),
  CONSTRAINT usuarios_permitidos_pkey PRIMARY KEY (id)
);